spring:
  datasource:
    url: "jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL;INIT=CREATE SCHEMA IF NOT EXISTS worklinker\\;SET SCHEMA worklinker"
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  cache:
    type: none